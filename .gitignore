# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Environment variables
.env
.env.gpg

# Runtime output
market_snapshot.json
collection_journal.jsonl
checkpoints.db
investment_brief_*.txt

# Log files (keep the logs/ dir itself via logs/.gitkeep)
logs/*.log

# Deploy templates render output (regenerated by Makefile each install)
deploy/rendered/

# uv lock (optional — include if you want reproducible installs)
# uv.lock

# Claude Code internal state
.claude/
